import PropTypes from "prop-types"
import React from "react"

const Header = () => (
  <header
    style={{
      background: `#085064`,
    }}
  >
    <div
      style={{
        padding: `1rem 0`,
      }}
    ></div>
  </header>
)

export default Header
